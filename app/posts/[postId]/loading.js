export default function Loading() {
  return (
    <h1
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        marginTop: "15rem",
      }}
    >
      Loading...
    </h1>
  );
}
